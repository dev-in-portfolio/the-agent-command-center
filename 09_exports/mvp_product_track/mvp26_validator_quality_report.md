# MVP-26

## Validator Quality Review

- Direct validator checks dashboard markers.
- Direct validator checks synthesis workspace and theme clustering models.
- Direct validator checks no service-role usage.
- Direct validator checks no token persistence.
- Direct validator checks no direct browser Supabase calls.
- Direct validator checks no update/delete/execute automation path.
- E2E validator requires the direct validator source to contain the required checks.
- E2E validator also runs the MVP-25 E2E validator and the master wall.
- SERVICE_ROLE_NOT_USED
- PASS_WITH_READ_ONLY_MANUAL_SYNTHESIS
