# MVP-27

- DECISION_TO_REQUEST_PAYLOAD_PREVIEW_READY
- Payload preview stays read-only until a manual operator decision.
- Preview references `POST /api/requests?action=create` as the server-gated path.
